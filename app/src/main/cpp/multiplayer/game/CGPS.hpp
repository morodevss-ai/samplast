// Created by bkuzn on 23.01.2026.#include "jni.h"#include "main.h"#include "Vector.h"class GPS {public:    static void DoPathDraw();    static void Set(CVector pos, bool toggle);    static bool IsEnabled();    static void Clear();    GPS();private:    static void RenderMarker();    static inline CVector m_to = CVector{0.0f, 0.0f, 0.0f};    static inline bool m_enabled = false;    static inline RwTexture* m_marker = nullptr;};